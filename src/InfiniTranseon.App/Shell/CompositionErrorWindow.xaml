<Window
    x:Class="InfiniTranseon.App.Shell.CompositionErrorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Infini-Transeon — startup error">
    <Grid Padding="32" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Spacing="12">
            <FontIcon Glyph="&#xEA39;" FontSize="24" Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
            <TextBlock
                x:Uid="CompositionErrorTitle"
                FontSize="20"
                FontWeight="SemiBold"
                VerticalAlignment="Center" />
        </StackPanel>
        <TextBlock
            x:Name="CompositionErrorBody"
            x:Uid="CompositionErrorBody"
            Grid.Row="1"
            TextWrapping="Wrap" />
        <ScrollViewer Grid.Row="2" Margin="0,4,0,0">
            <TextBox
                x:Name="DetailsBox"
                AutomationProperties.LabeledBy="{x:Bind CompositionErrorBody}"
                IsReadOnly="True"
                AcceptsReturn="True"
                TextWrapping="Wrap"
                FontFamily="Cascadia Mono"
                BorderThickness="1"
                Padding="12" />
        </ScrollViewer>
    </Grid>
</Window>
