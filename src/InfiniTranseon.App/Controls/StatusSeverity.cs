namespace InfiniTranseon.App.Controls;

// Kept free of WinUI types so presentation models, view models, and tests can share it.
public enum StatusSeverity
{
    Neutral,
    Success,
    Warning,
    Critical,
    Info,
}
