# 发布与客户验收

本应用目前采用用户自带服务凭据的桌面软件模式。购买软件不代表包含第三方翻译额度；没有内置支付、账户或订阅计费服务。收费发行前必须向客户说明服务费用、支持范围和退款规则，不应承诺所有游戏、语言或模型都达到同一翻译质量。

## 可复现的正式发布

以 `.github/workflows/build-release.yml` 为发布流程的唯一实现来源。它从不可变的 `vX.Y.Z` 标签运行托管及原生测试，构建自包含程序，生成模型目录、依赖清单和第三方声明，校验 MSI 与便携版，再签名更新清单。

正式发布需要 `release` 环境中的 `RELEASE_ED25519_PRIVATE_KEY` 和 `RELEASE_ED25519_KEY_ID`；签名必须匹配应用内置公钥，序号必须高于已发布版本。不要提交私钥、复用旧版本清单或跳过签名验证。触发带发布步骤的工作流会对外发布 GitHub Release，需要发布负责人批准。

正式版本的完整资产包括：

- `Infini-Transeon.msi`、`Infini-Transeon-portable.zip` 和对应标签的 `Infini-Transeon-source.zip`。
- `release-manifest.json`，其签名、版本、大小和 SHA-256 必须对应本次安装包。
- `sbom.json`、`THIRD-PARTY-NOTICES.json`、应用许可证以及原生依赖许可证；SBOM 生成时使用 `-ilt` 保留包内许可证文本。
- 程序旁的已签名 `model-catalog.json`、引擎、模型工作进程及原生运行库。本地模型由用户确认后下载，不随安装包预装。

本地构建或候选安装包不等于正式发布。未提交的工作区源码不能用 `git archive HEAD` 冒充对应源码包。Ed25519 更新签名也不等于 Windows Authenticode 代码签名；当前流程明确发布未 Authenticode 签名的二进制，Windows 仍可能显示“未知发布者”。

## 发给付费客户前的验收

在干净的 Windows 11 x64 设备上记录版本、系统版本、缩放、游戏、语言、服务/模型和结果；记录不得包含密钥或客户内容。下面每项需要实际证据，单元测试不能替代实机验收。

| 场景 | 通过标准 |
| --- | --- |
| 安装、升级、卸载 | MSI 安装后能启动；旧版本升级成功；升级失败能回滚；旧版本不能覆盖新版本；卸载后其他应用和用户数据不受影响。 |
| 便携版 | 解压后可独立启动，未安装开发工具和 .NET SDK 也能运行；用户数据写入 LocalAppData。 |
| 首次使用 | 新用户能添加服务、完成英文连接样例、选游戏窗口、识别文本并看到译文；未配置、无余额、错误密钥和离线状态有可理解的恢复提示。 |
| 真实在线翻译 | 用目标用户实际语言和游戏语句测试短句、长段、多行、人名、否定、数字、术语表、上下文；核对意思与完整性，修改术语后不复用旧结果。 |
| 真实本地翻译 | 用户确认下载后验证模型哈希及加载，断网完成受支持语言的 OCR 和翻译；不支持的语言有明确提示。 |
| 异常恢复 | 取消、超时、限流、断流、空响应、额度耗尽和断网均不能显示成功或缓存残缺译文；恢复网络后可继续。 |
| 游戏内行为 | 窗口化及无边框游戏可捕获；覆盖层和热键不抢焦点；切换窗口、DPI、显示器、暂停和恢复后位置正确。 |
| 可读性与可访问性 | 中英文、浅深主题、高对比度、200% 文字缩放、960×600 与 1280×720；设置、引导和坐标标签完整；键盘与读屏可操作。 |
| 隐私与更新 | 严格离线不发云请求；导出档案无凭据；无效签名、错误哈希和降级更新被拒绝；有效正式更新可安装。 |

## 收费发行边界

项目的 Apache-2.0 许可证允许商业使用，但发行时仍须履行许可证和归属声明义务。依赖白名单检查只验证已记录的许可证，不代替服务商条款、模型来源、商标及销售安排的审核。参见 [Apache 官方许可证 FAQ](https://www.apache.org/foundation/license-faq.html) 和仓库 [LICENSE](LICENSE)。

真实服务费用、发布密钥、Windows 代码签名、销售渠道、客户支持及退款政策由产品负责人配置。不得把测试替身的成功、未检查的界面或未签名候选包描述为已经完成客户验收。
